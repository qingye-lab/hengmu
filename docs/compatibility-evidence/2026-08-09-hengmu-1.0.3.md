# Hengmu 1.0.3 host compatibility evidence

This report is time-bound release evidence. It records only observed behavior
on the named host and does not imply equivalent activation in another client or
future client version.

## Codex CLI

| Field | Observed value |
| --- | --- |
| Date | 2026-08-09 |
| Operating system | macOS 27.0 (26A5388g), arm64 |
| Client | Codex CLI 0.147.0 |
| Package | `hengmu-1.0.3.zip` |
| Package SHA-256 | `7e0ec2169bf7025c7fab6668aff9461675065268c8df8155910b8866f0f998b7` |
| Built source | `4cab26892b1fa0ed7f4ca3e20c69a46874991382` |
| Install path | Temporary local marketplace `hengmu-v103-final-smoke`; Codex cache `/Users/tangyuan/.codex/plugins/cache/hengmu-v103-final-smoke/hengmu/1.0.3` |
| Invocation | `$hengmu` with an evidence-backed repository-audit request |
| Routed Skill | `project-architecture-audit` |
| Result | Pass: Codex loaded the installed 1.0.3 router and returned the focused project-audit route |

The route was executed with an ephemeral, read-only Codex task. The observed
response was:

```text
ROUTED_SKILL=project-architecture-audit
ROUTE_REASON=It owns evidence-backed repository audits and supports the requested read-only Advisory mode.
```

The pre-existing personal Hengmu 1.0.0 installation was temporarily removed to
avoid a same-name routing collision, then restored after the 1.0.3 smoke. The
temporary 1.0.3 plugin and marketplace were removed after the observation.

## Portable package and unavailable hosts

The `hengmu-1.0.3-agent-plugins.zip` archive had SHA-256
`35e91f18013608cd6ae6bd5c0ede8b578c7eff2e3ad8973fd0e776ba0ae8961c`
and passed the repository's extracted Knowledge Selection smoke test. This is
package evidence, not installed-host evidence.

Cursor, VS Code, GitHub Copilot CLI, and Kiro executables or applications were
not available on this machine, so no Hengmu 1.0.3 activation claim is made for
those hosts. ChatGPT desktop 26.803.41515 was installed, but this run did not
install or route Hengmu through its graphical plugin surface; that outcome
remains unverified.

## Reproduction boundary

The native observation used a local marketplace containing the exact built ZIP
and then ran Codex in a read-only task. Repository CI separately verifies both
archives' deterministic contents, extracted Knowledge Selection path,
checksums, and SPDX SBOM generation. A future host or Hengmu package-contract
change requires a new dated report rather than reusing this one.
