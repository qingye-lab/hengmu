# Hengmu 1.0.4 host compatibility evidence

This report is time-bound release evidence. It records only observed behavior
on the named host and does not imply equivalent activation in another client or
future client version.

## Codex CLI

| Field | Observed value |
| --- | --- |
| Date | 2026-08-10 |
| Operating system | macOS 27.0 (26A5388g), arm64 |
| Client | Codex CLI 0.147.0 |
| Package | `hengmu-1.0.4.zip` |
| Package SHA-256 | `2687365171f38f2ab00f072212cce0b7cc8ecaae9ff109c840f5ca08e9e0a15b` |
| Built source | `45c427396a7eb5fc500ed2ab4b6375720bfc310c` |
| Install path | Temporary local marketplace `hengmu-v104-smoke`; Codex cache `/Users/tangyuan/.codex/plugins/cache/hengmu-v104-smoke/hengmu/1.0.4` |
| Invocation | `$hengmu` with an evidence-backed repository-audit routing request |
| Routed Skill | `hengmu:project-architecture-audit` |
| Result | Pass: Codex loaded the installed 1.0.4 router and returned the focused project-audit route |

The route was executed with an ephemeral, read-only Codex task. The observed
response was:

```text
ROUTED_SKILL=hengmu:project-architecture-audit
ROUTE_REASON=Read-only, evidence-backed architecture audit of a single repository.
```

The pre-existing personal Hengmu 1.0.0 installation was temporarily removed to
avoid a same-name routing collision, then restored after the 1.0.4 smoke. The
temporary 1.0.4 plugin and marketplace were removed after the observation.
Unrelated installed plugins emitted manifest and unavailable-MCP warnings; they
did not prevent Hengmu installation, discovery, or routing.

## Portable package and unavailable hosts

The `hengmu-1.0.4-agent-plugins.zip` archive had SHA-256
`d6a0dacea7f11eb35a144414e3249f89b1fb7ebad31f24f4201a9a00574ee5c0`
and passed the repository's extracted Knowledge Selection smoke test. This is
package evidence, not installed-host evidence.

Cursor, VS Code, GitHub Copilot CLI, and Kiro executables or applications were
not available on this machine, so no Hengmu 1.0.4 activation claim is made for
those hosts. ChatGPT desktop 26.803.41515 was installed, but this run did not
install or route Hengmu through its graphical plugin surface; that outcome
remains unverified.

## Reproduction boundary

The native observation used a local marketplace containing the exact built ZIP
and then ran Codex in a read-only task. Two independent builds produced
byte-identical native and portable archives. Repository CI separately verifies
both archives' deterministic contents, extracted Knowledge Selection path,
checksums, and SPDX SBOM generation. A future host or Hengmu package-contract
change requires a new dated report rather than reusing this one.
