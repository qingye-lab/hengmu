# Hengmu 1.0.3 Windows follow-up verified architecture review

Subject: Hengmu (`hengmu`)  
Audited source commit: `fba6b93822cca2eb9c763b4949a95b07127251a9`  
Verification date: 2026-08-09  
Scope: repository root (`.`)  
Profile: `.architecture/reviews/inputs/2026-08-09-release-1.0.3-windows-followup-profile.yaml`  
Verifier: OpenAI `gpt-5.6-sol`, skeptical follow-up run `hengmu-1.0.3-windows-followup-verifier-20260809t025930z-gpt56sol`, level V2

## Strengths

- Hengmu has a coherent local plugin architecture: a stable public router delegates to eight focused Skills; shared schemas, contracts, Knowledge, Rule Packs, and a deterministic Python CLI live under `resources/`; and native Codex and portable Agent Plugins packages retain separate discovery manifests while sharing workflow outcomes.
- The workflow boundaries are explicit: the router delegates, candidate findings cannot gate, verification remains separate from solution selection, and remediation requires an accepted, source-bound decision.
- Deterministic provider execution rejects shell and package-runner entry points, binds executable/configuration/dependency/output hashes, uses isolated caches, validates structured output, and records dirty-tree state. The source-matched `test-results` run revalidated successfully and reports 161 passing tests with no failures, errors, or skips.
- Package construction uses an explicit runtime allowlist and fixed ZIP metadata. Extraction rejects duplicate names, traversal, absolute paths, backslashes, Windows drive forms, and symlinks; the unsafe-name fixture now preserves raw archive bytes even on Windows, and YAML contract tests select UTF-8 explicitly.
- Tag release depends on all six Linux/macOS/Windows and Python 3.11/3.13 quality jobs, then independently reruns repository, history, Knowledge, test, release-gate, lint, dependency, license, build, smoke, checksum, SBOM, provenance, and per-package SBOM-attestation steps.
- Host claims are bounded to observed evidence. The dated report records one Codex CLI 0.147.0 macOS installation and route, restores prior state, and explicitly leaves other clients unverified.
- Both 30-trial model benchmark runs revalidated in archived mode against their source commit, context manifest, fixture trees, command vectors, execution logs, runtime records, and the audited artifact commit. Diagnostic runs remain excluded from release evidence.

## Confirmed risks

None. The strongest counter-hypothesis—that a documented or happy-path-only release claim concealed an executable bypass, unsafe archive edge case, stale benchmark binding, incomplete matrix dependency, or host-equivalence overclaim—was contradicted by direct source, adversarial tests, workflow structure, history, and validated deterministic evidence.

## Limitations

- This is V2 agent verification. No human verifier participated, so the deterministic provider evidence does not elevate the result to V4.
- Hosted CI exposed the two corrected Windows-only test portability defects. The source-matched deterministic Provider reports 161 passing tests; a post-follow-up hosted rerun and actual release publication remain pending.
- Host evidence covers only Codex CLI 0.147.0 on the recorded macOS host. Other host activation remains unknown.
- The archived benchmark records validate, but the currently installed Codex runtime version differs from the recorded 0.147.0 runtime; no current-runtime equivalence claim is made.

## Counts

Raw findings: 0. Confirmed: 0. Rejected: 0. Needs evidence: 0. Coverage: 31 Rule Pack rows exactly once (28 assessed, 3 not applicable), six assessed critical flows, and 16 selected Knowledge bindings; coverage is complete.
