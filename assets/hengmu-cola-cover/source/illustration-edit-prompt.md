# Hengmu cover illustration layer edit prompt

Use case: precise-object-edit

Asset type: non-text illustration layer for a 16:9 Qingye project Banner

Input image: Image 1 is the edit target.

Primary request: Move the complete technical linework group horizontally to the right so that the entire left 52% of the canvas is uninterrupted warm-white negative space.

Change only: horizontal placement of the existing linework group.

Preserve exactly: canvas ratio, warm-white background, all existing line fragments, open boundary planes, evidence points, convergence point, line count, relative geometry, scale, stroke hierarchy, graphite/gray palette, flat vector-like rendering, and clean crop.

Constraints: no line, point, plane, or generated object may appear in the left 52% of the canvas. Keep the full group visible inside the right 48%; do not crop the outer plane or endpoint. Add nothing. Remove nothing. No text, logo, wordmark, project name, slogan, blue, gradient, texture, shadow, watermark, icon, arrow, checkmark, building, shield, brain, circuit board, person, or UI.

Avoid: redesigning or restyling the linework; changing the background; changing geometry; adding decorative elements.
